.checkin-questions

  h3 = currentSectionPrompt

  each question in sectionQuestions
    = questioner-input type="catalog" question=question section=currentSection responses=responsesData action="setResponse"