label
  unless view.noLabelText
    = view.label

  each option in view.options
    div click="'set' option.value target=view" class={:radio-option option.checked:checked:unchecked}
      h6.option-name
        if option.checked
          |&#10003;
        = option.name

  partial "forms/errors"